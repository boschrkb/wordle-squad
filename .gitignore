node_modules/

# Data files — stored in data/ which is tracked, but the files themselves are not
data/*.json
data/*.json.tmp
data/*.db
data/*.db-shm
data/*.db-wal

# Legacy DB files in project root (safe to delete once migrated)
*.db
*.db-shm
*.db-wal

.DS_Store
.env
